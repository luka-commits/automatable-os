# Copy this file to context/config.yaml and fill it in — the setup skill does
# this for you interactively, but you can also edit it by hand.
#
# Flat key: value pairs only (no nesting) — the dashboard renderer parses this
# with a small hand-rolled reader, not a YAML library, to keep the repo
# dependency-free.

name: "Your Name"

# ---------------------------------------------------------------------------
# The Upwork add-on. Everything from here to the end of this block belongs to it.
# Say no during setup and this line goes to false, the dashboard drops its tab,
# the morning skill skips its Upwork pass, and none of the fields below matter.
upwork_enabled: true

# Your Upwork account/org identifier, needed for the screener's find_jobs
# calls. The setup skill fetches this for you automatically via the Upwork
# MCP connection — you shouldn't need to find it yourself.
upwork_org_uid: ""

# Your public Upwork profile. The pitch page's closing button points here, so
# without it the page ends on the paragraph and no button — which is the right
# failure, but a weaker page. Setup fills this in from your account.
upwork_profile_url: ""

# Optional, all three. They appear on a pitch page only if set: the portfolio
# under the work samples, the channel under the videos, the website in the
# footer. An unset one is left out rather than linked dead.
portfolio_url: ""
youtube_url: ""
website_url: ""

# Optional. Your own Slack user ID (for DM notifications on strong matches).
# Leave empty to skip Slack entirely — the screener still logs every
# candidate to the dashboard either way, Slack is just a push notification.
slack_channel_id: ""

# How many proposals you aim to send per working day. The dashboard's tracker
# counts against this, and it is the one number the whole pipeline exists to
# move. Pick something you will actually hit — a goal you miss daily stops
# being a goal after a week.
daily_proposal_goal: 5

# The platforms you actually build on, comma-separated. Two jobs: it sharpens
# the screener's niche fit, and it lets a skill tell you *which* credential is
# missing instead of failing vaguely. Free text, use the names clients use.
# Example: "gohighlevel, n8n, make, zapier"
tools: ""

# Your own standing on Upwork. Setup reads these off your account, so you do
# not type them. They are here because client minimums (minimum Job Success,
# minimum earnings, minimum hours) are checked against exactly these numbers —
# the dashboard can only tell you "you are under their bar, this proposal will
# never be shown" if it knows where you stand. Leave them empty and that
# judgement is omitted rather than guessed.
job_success:
lifetime_earnings:
hours_worked:
hourly_rate:
jobs_completed:
reviews:

# Connects are the real budget ceiling: a proposal costs between 7 and 22 of
# them, so the balance decides how many you get to send this month.
connects_balance:

# Free-text warnings shown on the "where you stand" card, in quotes. Leave
# empty and nothing is shown — an absent note means "no note", not "all clear".
timezone_warning: ""
connects_drain_note: ""

# API keys are NOT stored here and never in this repo. They live in
# ~/.config/credentials.env with chmod 600, one KEY=value per line, and every
# skill reads them from there. .gitignore already blocks credentials.env, .env
# and *.key — but the habit is what protects you, not the file.
# A skill that needs a key you have not set says which one and stops. It does
# not guess, and it does not carry on with half the picture.

# ---------------------------------------------------------------------------
# The cold mail add-on. Everything from here to the end of this block belongs
# to it. Say no during setup and this line goes to false, the dashboard drops
# its tab, and none of the fields below matter.
#
# Unlike Upwork, this add-on costs money before it earns any: mailboxes and
# domains at Zapmail, a sending subscription at Instantly, and per-niche scrape
# credits. `coldmail-setup` works out how many of each your target needs and
# states the monthly total before anything is bought.
coldmail_enabled: false

# The day warmup started. New mailboxes may not send for about two weeks; the
# dashboard counts the days from here so you can see whether you are clear yet.
# Written by coldmail-setup, format YYYY-MM-DD.
coldmail_warmup_started: ""

# How many mailboxes you had created, and what you want to send per day. The
# dashboard multiplies mailboxes by 30 (Instantly's per-mailbox ceiling) to show
# your real capacity. Raising the 30 is not an option — you scale by adding
# mailboxes across more domains, never by sending more from each.
coldmail_mailboxes: 0
coldmail_target_per_day: 0
